<%= form_for(@music_like) do |f| %>
  <% if @music_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@music_like.errors.count, "error") %> prohibited this music_like from being saved:</h2>

      <ul>
      <% @music_like.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :music_id %><br />
    <%= f.number_field :music_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
