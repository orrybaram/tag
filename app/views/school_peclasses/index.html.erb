<h1>Listing school_peclasses</h1>

<table>
  <tr>
    <th>School</th>
    <th>Peclass</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @school_peclasses.each do |school_peclass| %>
  <tr>
    <td><%= school_peclass.school_id %></td>
    <td><%= school_peclass.peclass_id %></td>
    <td><%= link_to 'Show', school_peclass %></td>
    <td><%= link_to 'Edit', edit_school_peclass_path(school_peclass) %></td>
    <td><%= link_to 'Destroy', school_peclass, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New School peclass', new_school_peclass_path %>
