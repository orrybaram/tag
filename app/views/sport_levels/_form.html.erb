<%= form_for(@sport_level) do |f| %>
  <% if @sport_level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sport_level.errors.count, "error") %> prohibited this sport_level from being saved:</h2>

      <ul>
      <% @sport_level.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :sport_id %><br />
    <%= f.number_field :sport_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
