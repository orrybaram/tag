<%= form_for(@user_fun) do |f| %>
  <% if @user_fun.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_fun.errors.count, "error") %> prohibited this user_fun from being saved:</h2>

      <ul>
      <% @user_fun.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :fun_id %><br />
    <%= f.number_field :fun_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
