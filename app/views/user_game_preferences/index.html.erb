<h1>Listing user_game_preferences</h1>

<table>
  <tr>
    <th>User</th>
    <th>Game</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_game_preferences.each do |user_game_preference| %>
  <tr>
    <td><%= user_game_preference.user_id %></td>
    <td><%= user_game_preference.game_id %></td>
    <td><%= link_to 'Show', user_game_preference %></td>
    <td><%= link_to 'Edit', edit_user_game_preference_path(user_game_preference) %></td>
    <td><%= link_to 'Destroy', user_game_preference, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User game preference', new_user_game_preference_path %>
